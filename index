<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Questionário - Vício em Telas</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body {
      font-family: "Orbitron", Arial, sans-serif;
      margin: 0; padding: 20px;
      background: linear-gradient(135deg, #0a0f1f, #000000);
      color: #d0e6ff;
    }
    h1, h2 {
      text-align: center;
      color: #00aaff;
      text-shadow: 0 0 10px #00aaff;
    }
    .legend-box {
      max-width: 500px;
      margin: 20px auto;
      padding: 15px;
      background: rgba(0, 20, 40, 0.9);
      border-radius: 12px;
      box-shadow: 0 0 12px rgba(0,170,255,0.7);
      font-size: 0.95rem;
    }
    .legend-box ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .legend-box li {
      margin: 5px 0;
      padding: 5px;
      border-left: 4px solid #00aaff;
    }
    .question {
      margin: 15px auto;
      padding: 20px;
      max-width: 600px;
      background: rgba(0, 20, 40, 0.9);
      border-radius: 15px;
      box-shadow: 0 0 15px rgba(0, 170, 255, 0.6);
    }
    label {
      display: block;
      margin-bottom: 8px;
      font-weight: bold;
    }
    select {
      width: 100%;
      padding: 10px;
      border-radius: 8px;
      border: none;
      background: #001a33;
      color: #00aaff;
      font-weight: bold;
    }
    button {
      margin: 20px auto;
      display: block;
      padding: 12px 20px;
      border: none;
      background: #00aaff;
      color: white;
      font-weight: bold;
      border-radius: 12px;
      cursor: pointer;
      box-shadow: 0 0 15px #00aaff;
      transition: 0.3s;
    }
    button:hover {
      background: #0088cc;
      box-shadow: 0 0 25px #00ccff;
    }
    #result {
      margin-top: 30px;
      text-align: center;
      display: none;
    }
    #chart-container {
      max-width: 500px;
      margin: 20px auto;
      background: rgba(0,0,0,0.7);
      padding: 15px;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0,170,255,0.8);
    }
    .btn-desafio {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 20px;
      background: #28a745;
      color: white;
      border-radius: 10px;
      text-decoration: none;
      font-weight: bold;
      box-shadow: 0 0 10px #28a745;
      transition: 0.3s;
    }
    .btn-desafio:hover {
      background: #1f7a33;
      box-shadow: 0 0 20px #33ff66;
    }
  </style>
</head>
<body>
  <h1>📱 Questionário sobre Vício em Telas</h1>

  <div class="legend-box">
    <h3>📌 Escala de Respostas:</h3>
    <ul>
      <li><strong>0 = Nunca</strong></li>
      <li><strong>1 = Raramente</strong></li>
      <li><strong>2 = Às vezes</strong></li>
      <li><strong>3 = Sempre</strong></li>
    </ul>
  </div>

  <form id="quiz-form">
    <div class="question">
      <label>1. Você sente necessidade de verificar o Instagram várias vezes ao dia?</label>
      <select name="instagram">
        <option value="0">Nunca</option>
        <option value="1">Raramente</option>
        <option value="2">Às vezes</option>
        <option value="3">Sempre</option>
      </select>
    </div>
    <div class="question">
      <label>2. Você passa horas assistindo vídeos no YouTube sem perceber o tempo?</label>
      <select name="youtube">
        <option value="0">Nunca</option>
        <option value="1">Raramente</option>
        <option value="2">Às vezes</option>
        <option value="3">Sempre</option>
      </select>
    </div>
    <div class="question">
      <label>3. Você sente ansiedade se fica muito tempo sem olhar o WhatsApp?</label>
      <select name="whatsapp">
        <option value="0">Nunca</option>
        <option value="1">Raramente</option>
        <option value="2">Às vezes</option>
        <option value="3">Sempre</option>
      </select>
    </div>
    <div class="question">
      <label>4. Você fica horas rolando o feed do TikTok?</label>
      <select name="tiktok">
        <option value="0">Nunca</option>
        <option value="1">Raramente</option>
        <option value="2">Às vezes</option>
        <option value="3">Sempre</option>
      </select>
    </div>
    <div class="question">
      <label>5. Você não consegue passar o dia sem ouvir música no Spotify?</label>
      <select name="spotify">
        <option value="0">Nunca</option>
        <option value="1">Raramente</option>
        <option value="2">Às vezes</option>
        <option value="3">Sempre</option>
      </select>
    </div>
    <div class="question">
      <label>6. Você atualiza o Twitter/X várias vezes ao dia?</label>
      <select name="twitter">
        <option value="0">Nunca</option>
        <option value="1">Raramente</option>
        <option value="2">Às vezes</option>
        <option value="3">Sempre</option>
      </select>
    </div>

    <button type="submit">Enviar Respostas</button>
  </form>

  <div id="result">
    <h2>📊 Sua Análise</h2>
    <div id="chart-container">
      <canvas id="resultChart"></canvas>
    </div>
    <p id="analysis-text"></p>
  </div>

  <script>
    const form = document.getElementById("quiz-form");
    const resultDiv = document.getElementById("result");
    const ctx = document.getElementById("resultChart").getContext("2d");
    const analysisText = document.getElementById("analysis-text");
    let chart;

    form.addEventListener("submit", function(e){
      e.preventDefault();

      const data = new FormData(form);
      let total = 0;

      data.forEach((value) => {
        total += Number(value);
      });

      // Máximo possível = 18 (6 perguntas * 3 pontos)
      let status = "";
      let scores = { "Equilibrado": 0, "Pouco Viciado": 0, "Viciado": 0 };

      if(total <= 6){
        status = "Equilibrado";
        scores["Equilibrado"] = 1;
      } else if(total <= 12){
        status = "Pouco Viciado";
        scores["Pouco Viciado"] = 1;
      } else {
        status = "Viciado";
        scores["Viciado"] = 1;
      }

      if(chart){ chart.destroy(); }

      chart = new Chart(ctx, {
        type: "pie",
        data: {
          labels: Object.keys(scores),
          datasets: [{
            data: Object.values(scores),
            backgroundColor: ["#00ffcc","#ffaa00","#ff0033"]
          }]
        },
        options: {
          responsive: true,
          plugins: {
            legend: { position: "bottom" }
          }
        }
      });

      analysisText.innerHTML = `🔎 Resultado: Você está <strong>${status}</strong> no uso de telas.`;
      resultDiv.style.display = "block";
      window.scrollTo({ top: document.body.scrollHeight, behavior: "smooth" });
    });
  </script>
</body>
</html>
